extends Node

@export var max_hp = 100
var curr_hp = max_hp

@onready var health_bar = $HealthController

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass
